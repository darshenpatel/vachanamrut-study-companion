# Minimal dependency set for local smoke testing (no heavy ML deps).
# Intended for environments where Python is 3.13+ and packages like faiss/torch
# may not have readily available wheels.
#
# Usage:
#   cd backend
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements-smoke.txt
#
# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
pydantic-settings==2.0.3
httpx==0.25.2

# PDF processing and text extraction (used by factual retrieval)
pdfplumber==0.10.3

# Runtime utility deps (imported by cache_manager)
psutil==5.9.8

# Needed for importing semantic search modules (even in fallback mode).
numpy>=2.1.0,<3.0


